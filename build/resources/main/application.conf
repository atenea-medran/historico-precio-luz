ktor {
    development = true
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.ateneamedran.ApplicationKt.module ]
    }
}

storage {
    driverClassName = "com.mysql.cj.jdbc.Driver"
    jdbcURL = "jdbc:mysql://localhost:3306/kwh"
    username = "root"
    password = ".Ciclopentano69"
}